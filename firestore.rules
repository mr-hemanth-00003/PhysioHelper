rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to everyone for the articles collection
    match /articles/{document=**} {
      allow read, write: if true;
    }
  }
}
